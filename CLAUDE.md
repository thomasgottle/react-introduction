# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository currently contains only `README.md` — there is no source code, package manifest, or build tooling yet. Nothing can be built, run, or tested at this point.

## Purpose

A learning project following the Next.js [React Foundations](https://nextjs.org/learn/react-foundations) course. That course starts from a single HTML file that loads React and Babel from a CDN, then incrementally migrates the same app to Next.js (installing `next`/`react`/`react-dom`, moving markup into `app/page.js`, adding components, props, state, and finally server vs. client components).

Because the course is incremental, the toolchain here changes as the project progresses. When adding code, follow the stage the course is at rather than jumping ahead to a full framework setup.

## Maintaining this file

Once real tooling exists (e.g. a `package.json` with `next dev` / `next build`, or a test runner), replace this section with the actual commands and an architecture overview.
